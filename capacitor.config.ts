import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.juanmigonzalez.gamery',
  appName: 'Gamery',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
