import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'my-game-library',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
